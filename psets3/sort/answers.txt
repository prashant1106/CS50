sort1 uses: Bubble sort

How do you know?: Takes 5.430s to sort reversed list; Takes 0.458s to sort sorted list; O() was highest i.e. for random and reversed files but Ω() was lower than that of sort3 for sorted files.

sort2 uses: Merge sort

How do you know?: Takes 0.678s to sort reversed list; Takes 0.526s to sort sorted list; It had the least running time for random and reversed files that meast lowest O() so it is merge sort.

sort3 uses: Selection sort

How do you know?: Takes 2.572s to sort reversed list; Takes 2.179s to sort sorted list; It had quite higher O() than sort2 but quite less than sort1 and it had Ω() higher than sort1 that is for the sorted files. So, it is selction sort.
